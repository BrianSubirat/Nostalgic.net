// REMOVED: Lights Out game has been completely deleted as requested
export const PUZZLE_GAMES = [];

document.addEventListener('DOMContentLoaded', () => {
    const gamesContainer = document.getElementById('games');
    
    // No games to add since array is empty
});

export default PUZZLE_GAMES;